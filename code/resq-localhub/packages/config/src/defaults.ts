export const DEFAULTS = {
  MQTT_PORT: 1883,
  MQTT_WS_PORT: 9001,
  API_PORT: 8080,
  HUB_NAME: 'resq-hub',
  DB_PATH: './data/resq.db',
  LOG_LEVEL: 'info',
};// default configuration values
// TODO: export defaults for port, paths, etc.
