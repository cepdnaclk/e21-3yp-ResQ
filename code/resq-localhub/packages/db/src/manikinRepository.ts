import db from './client';
// TODO: Add queries for CRUD operations
export class ManikinRepository {
  // Example: get all manikins
  getAll() {
    // SELECT * FROM manikins
  }
  // Example: add manikin
  add(manikin: any) {
    // INSERT INTO manikins ...
  }
  // Example: update manikin
  update(id: string, updates: any) {
    // UPDATE manikins SET ... WHERE id = ?
  }
  // Example: delete manikin
  delete(id: string) {
    // DELETE FROM manikins WHERE id = ?
  }
}