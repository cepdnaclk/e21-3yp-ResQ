import { FastifyPluginAsync } from 'fastify';
const websocketPlugin: FastifyPluginAsync = async (app) => {
  // TODO: Setup websocket for live telemetry/session updates
};
export default websocketPlugin;import { FastifyPluginAsync } from 'fastify';
const websocketPlugin: FastifyPluginAsync = async (app) => {
  // TODO: Setup websocket for live telemetry/session updates
};
export default websocketPlugin;import { FastifyPluginAsync } from 'fastify';
const websocketPlugin: FastifyPluginAsync = async (app) => {
  // TODO: Setup websocket for live telemetry/session updates
};
export default websocketPlugin;// plugin to set up websocket or SSE for live updates
// TODO: implement connection handling and broadcast
