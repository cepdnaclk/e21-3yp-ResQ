/**
 * auth.ts — Auth types for V2.
 * Re-exports from @resq/shared to avoid duplication.
 */
export type {
  AuthUser,
  UserRole,
  LoginRequest,
  LoginResponse,
  AuthStatusResponse,
  AuthBootstrapResponse,
  CreateFirstAdminRequest,
  CreateUserRequest,
} from "@resq/shared";
